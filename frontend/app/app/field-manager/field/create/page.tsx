import CreateFieldForm from "@/components/create-field/create-field-form";
import React from "react";

const page = async () => {
  return (
    <div>
      <CreateFieldForm />
    </div>
  );
};

export default page;
